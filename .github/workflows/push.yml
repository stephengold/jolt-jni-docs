---
# GitHub Actions workflow for commits pushed to the jolt-jni-docs repo - all branches

name: CI at GitHub
on: [push]

jobs:
  Java11-Linux:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 11
      - uses: actions/checkout@v4
      - uses: gradle/actions/wrapper-validation@v4
      - run: ./gradlew build javadoc HelloJoltJni --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          lein: 2.11.2
      - run: lein run

  Java17-MacOS:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macOS-13
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - uses: actions/checkout@v4
      - run: ./gradlew build javadoc HelloJoltJni --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          lein: 2.11.2
      - run: lein run

  Java21-MacOS:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macOS-15
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 21
      - uses: actions/checkout@v4
      - run: ./gradlew build javadoc HelloJoltJni --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          lein: 2.11.2
      - run: lein run

  Java24-Windows:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: windows-2025
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 24
      - uses: actions/checkout@v4
      - run: ./gradlew build javadoc HelloJoltJni --console=plain --stacktrace
        shell: bash
      - uses: DeLaGuardo/setup-clojure@13.4
        with:
          lein: 2.11.2
      - run: lein run

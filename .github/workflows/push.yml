---
# GitHub Actions workflow for commits pushed to the jolt-jni-docs repo - all branches

name: CI at GitHub
on: [push]

jobs:
  Java17-Linux:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: 17
      - uses: actions/checkout@v6
      - uses: gradle/actions/wrapper-validation@v5
      - run: ./gradlew build install javadoc HelloJoltJni -x :java-apps:checkstyleMain --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.6.0
        with:
          lein: 2.12.0
      - run: lein run

  Java21-MacOS:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macos-26
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: 21
      - uses: actions/checkout@v6
      - run: ./gradlew build install javadoc HelloJoltJni --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.6.0
        with:
          lein: 2.12.0
      - run: lein run

  Java25-Windows:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: windows-2025
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: 25
      - uses: actions/checkout@v6
      - run: ./gradlew build install javadoc HelloJoltJni --console=plain --stacktrace
        shell: bash

# DeLaGuardo/setup-clojure isn't compatible with Windows

  Java25-MacOS:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macos-26-intel
    steps:
      - uses: actions/setup-java@v5
        with:
          distribution: 'liberica'
          java-version: 25
      - uses: actions/checkout@v6
      - run: ./gradlew build install javadoc HelloJoltJni -x :java-apps:checkstyleMain --console=plain --stacktrace
      - uses: DeLaGuardo/setup-clojure@13.6.0
        with:
          lein: 2.12.0
      - run: lein run
